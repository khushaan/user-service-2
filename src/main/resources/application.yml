spring:
  application:
    name: user-service

    server:
      port: 8080

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8307/eureka/
  instance:
    hostname: localhost